<%= form_for(@house_book) do |f| %>
  <% if @house_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@house_book.errors.count, "error") %> prohibited this house_book from being saved:</h2>

      <ul>
      <% @house_book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br/>
    <%= f.text_field :name %>
  </p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
